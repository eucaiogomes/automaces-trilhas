# 📄 11.1 - Corrigir Avaliação (Lógica Administrativa)

### 🎯 Objetivo do Script
Validar a funcionalidade de correção manual de notas por parte do Administrador.

### 🧠 Lógica do Código: Localização Precisa
Este script foca na precisão para gerenciar a turma correta:

1.  **Busca de Turma por Nome**: O código utiliza o `cy.contains('b.ng-binding', 'Gratuita')` para encontrar o container específico da turma desejada dentro de uma lista que pode ter várias turmas.
2.  **Navegação Hierárquica**: Ele usa o comando `.closest('div[ng-repeat...]')` para encontrar o "quadrado" inteiro da turma e depois clica no botão "Gerenciar" que está lá dentro. Isso garante que ele não clique no botão "Gerenciar" da turma errada.
3.  **Inserção de Nota e Feedback**: O código preenche o campo de nota e o campo de observações. Ele usa o `.clear()` antes do `.type()` para garantir que qualquer valor antigo seja apagado antes de digitar a nota 10.

### 🛠️ Estrutura Técnica
O script valida se o administrador realmente logou e se está no perfil correto antes de tentar acessar a URL da trilha, usando uma lógica de segurança no início do teste.
