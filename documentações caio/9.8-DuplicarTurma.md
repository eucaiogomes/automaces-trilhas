# 📄 9.8 - Duplicar Turma (Lógica de Repetição)

### 🎯 Objetivo do Script
Testar a estabilidade da função de duplicação de turmas ao criar múltiplas turmas iguais em sequência.

### 🧠 Lógica do Código: O Loop `for`
Neste arquivo, utilizamos uma lógica de programação chamada **Loop (laço de repetição)**.

-   **`for (let i = 1; i <= 5; i++)`**: Esta linha diz ao sistema: *"Repita tudo o que está aqui dentro 5 vezes"*. 
-   **Diferenciação por Nome**: Para não criar 5 turmas com o mesmo nome, o código usa a variável `${i}` para dar nomes diferentes, como "editada caio 1", "editada caio 2", etc.

### 🏠 Estrutura Técnica
-   **`beforeEach`**: Diferente de outros scripts, este usa o `beforeEach`, que faz o login **antes de cada uma** das 5 duplicações. Isso garante que, se uma duplicação falhar, a próxima comece de um estado limpo.
-   **Seletores Dinâmicos**: O código usa o comando `.first()` para garantir que ele sempre tente duplicar a primeira turma da lista, facilitando a lógica de repetição.
