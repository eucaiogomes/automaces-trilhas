# 📄 9.92 - Adicionar Certificado (Lógica de Lista de Dados)

### 🎯 Objetivo do Script
Testar a substituição de diversos modelos de certificado em uma mesma trilha.

### 🧠 Lógica do Código: Lista de Variáveis
Em vez de escrever o nome do certificado direto no comando, o código usa uma **Constante de Lista** (`const certificados`).

-   **Otimização**: O nome de todos os certificados que queremos testar fica no topo do código. O `for` (loop) percorre essa lista e troca o certificado sozinho.
-   **Busca em Dropdown**: O código abre o menu de seleção (`.ui-select-toggle`), digita o nome do certificado atual da lista e clica na primeira sugestão que aparece (`.ui-select-highlight`).

### 🛠️ Estrutura Técnica
-   **`cy.log`**: Ao final de cada troca, o código gera uma mensagem no painel do Cypress confirmando qual certificado foi adicionado. Isso ajuda muito na hora de conferir se o teste deu certo.
